public class Comparator {
    private Comparator(){};

    public static StudentEnum(StudentEnum studentEnum){
        switch (studentEnum){
            case AVG_EXAM_SCORE -> {return new StudentAvgExamScoreComparator();
            }

            }
        }
    }
}
